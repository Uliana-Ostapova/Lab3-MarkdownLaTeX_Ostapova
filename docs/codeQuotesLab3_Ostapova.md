**Блоки кода**
Встроенный (inline) код:
Команда `git status` показывает состояние репозитория.
Многострочный код:
```python
print("Hello, Markdown!")
```

Можно указать язык: python, js, bash, csharp и др.

**Цитаты**
Цитата создаётся символом >:
> Это цитата в Markdown.
Вложенная цитата:
> Первый уровень
>> Второй уровень
Создание и редактирование файла Создайте файл:
touch codeQuotesLab3_FIO.md Добавьте примеры:
•	inline-кода,
•	блоков кода с подсветкой,
•	обычных блоков кода,
• цитат.
___
**Пример команды Git в виде inline-кода:**
команда `cd ..` позволяет перейти на один уровень выше в структуре папок.

**Блоки кода:**
с указанием языка (python):
```python
name = "Uliana"
print(f"Hello, {name}!")
```
без указания языка (C#):
```
Console.WriteLine("Привет Мир!");
```
**Цитаты:**
> Обычная цитата.

> Вложенная цитата:
первый уровень.
>> Второй уровень.